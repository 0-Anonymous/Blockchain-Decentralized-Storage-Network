<Page x:Class="DeezFiles.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:DeezFiles.Models"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d" 
      Background="#212121"
      Title="MainPage">

    <Page.Resources>
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#f4f4f4"/>
            <Setter Property="Background" Value="#1e1e28"/>
            <Setter Property="BorderBrush" Value="#c9a9ff"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="MinWidth" Value="90"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="ButtonBorder"
                                    CornerRadius="12"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="6" ShadowDepth="2" Opacity="0.18" Color="#000000"/>
                                </Border.Effect>
                            </Border>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#2d2638"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#e0ceff"/>
                                <Setter Property="Foreground" Value="#e0ceff"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#2a2233"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#d7bcff"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="UploadButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#241b33"/>
            <Setter Property="Background" Value="#c9a9ff"/>
            <Setter Property="BorderBrush" Value="#e0ceff"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Width" Value="154"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                CornerRadius="18"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#d9c2ff"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#f1e7ff"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#b693f7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.65"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="16,16,16,10" MaxWidth="1010" HorizontalAlignment="Stretch">
            <TextBlock x:Name="DFTarget" Text="DEEZFILES" FontFamily="Segoe UI" FontSize="36" Foreground="White" FontWeight="Bold" Margin="16,16,16,10" HorizontalAlignment="Center"/>
            <Grid Height="Auto" MinHeight="50">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="RefreshButton" Grid.Column="0" Click="Refresh_Click" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,16,0" ToolTip="Refresh file list">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="13" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Refresh" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Border Grid.Column="1" Height="1" Background="#2f2f34" HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
                <Button x:Name="LogOut" Grid.Column="2" Click="LogOut_Click" Style="{StaticResource ToolbarButtonStyle}" Margin="16,0,0,0" ToolTip="Sign out">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Sign out" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <Image Source="pack://application:,,,/Assets/logout.png" Height="16" Width="16" Stretch="Fill" Margin="8,0,0,0"/>
                    </StackPanel>
                </Button>
            </Grid>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="24,10,24,24" HorizontalAlignment="Stretch">
            <TextBlock x:Name="UserID" Text="VaderPooperFader66" FontSize="18" FontFamily="Roboto" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top" Foreground="White"/>
            <TextBlock x:Name="UserAdd" Text="@vndjnsknsdn" FontSize="12" Foreground="#585858" HorizontalAlignment="Center"/>

            <Grid Margin="0,24,0,20" Background="#252525" Height="320" MaxWidth="900" MinWidth="420" HorizontalAlignment="Stretch">
                <Grid.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="30"/>
                    </Style>
                </Grid.Resources>
                <DockPanel Margin="10">

                    <!-- Headers -->
                    <Grid DockPanel.Dock="Top" Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="5*" />
                            <ColumnDefinition Width="1.8*" MinWidth="130" />
                            <ColumnDefinition Width="1*" MinWidth="75" />
                            <ColumnDefinition Width="1.2*" MinWidth="105" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="File Name" Grid.Column="0" FontWeight="Bold" Foreground="LightGray" Margin="30,0,8,0"/>
                        <TextBlock Text="Date" Grid.Column="1" FontWeight="Bold" Foreground="LightGray" HorizontalAlignment="Center"/>
                        <TextBlock Text="Size" Grid.Column="2" FontWeight="Bold" Foreground="LightGray" HorizontalAlignment="Center"/>
                        <TextBlock Text="Actions" Grid.Column="3" FontWeight="Bold" Foreground="LightGray" HorizontalAlignment="Center"/>
                    </Grid>

                    <!-- Scrollable rows container -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto" DockPanel.Dock="Bottom">
                        <StackPanel x:Name="RowsPanel" Orientation="Vertical" />
                    </ScrollViewer>

                </DockPanel>
                <TextBlock Name="TotalFilesCount" Text="Total Files: 0" FontSize="12" FontFamily="Segoe UI" FontWeight="Bold" Foreground="#535353" Margin="10" VerticalAlignment="Bottom"/>
            </Grid>

            <Grid MaxWidth="600" HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <StackPanel HorizontalAlignment="Center" Margin="16,0">
                        <TextBlock Text="Total Files" FontSize="12" FontFamily="Segoe UI" FontWeight="SemiBold" Foreground="#535353" HorizontalAlignment="Center"/>
                        <TextBlock Text="Uploaded" FontSize="12" FontFamily="Segoe UI" FontWeight="SemiBold" Foreground="#535353" HorizontalAlignment="Center"/>
                        <TextBlock Name="UploadSizeTotal" Text="0KB" FontSize="12" FontFamily="Segoe UI" FontWeight="Bold" Foreground="#7e7e7e" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <Button x:Name="Upload" Click="Upload_Click" FontSize="16" FontFamily="Segoe UI" FontWeight="Bold" Style="{StaticResource UploadButtonStyle}" Margin="16,0" ToolTip="Upload a file">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Upload File" FontSize="14" Foreground="#241b33" FontWeight="Bold" VerticalAlignment="Center"/>
                            <Image Source="pack://application:,,,/Assets/Upload.png" Height="18" Width="18" Stretch="Fill" Margin="10,0,0,0"/>
                        </StackPanel>
                    </Button>

                    <StackPanel HorizontalAlignment="Center" Margin="16,0">
                        <TextBlock Text="Total Storage" FontSize="12" FontFamily="Segoe UI" FontWeight="SemiBold" Foreground="DarkGray" HorizontalAlignment="Center"/>
                        <TextBlock Text="In Use" FontSize="12" FontFamily="Segoe UI" FontWeight="SemiBold" Foreground="#535353" HorizontalAlignment="Center"/>
                        <TextBlock Text="1.6GB" FontSize="12" FontFamily="Segoe UI" FontWeight="Bold" Foreground="#7e7e7e" HorizontalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
